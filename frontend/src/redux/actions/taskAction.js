export const TASKS = 'TASKS';

export const ORDER = 'ORDER';

export const TASK = 'TASK';

export const actionTasks = (state) => ({ type: TASKS, state });

export const actionOrder = (state) => ({ type: ORDER, state });

export const actionTask = (state) => ({ type: TASK, state });
